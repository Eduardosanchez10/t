//Crear un array de strings con la primera letra de cada palabra de un array de strings.
const Array = [ 'alberto', 'luis', 'david', 'carina' ];
const  Narray=[]
for (let I = 0; I < Array.length; I++) {
    Narray.push(Array[I][0]);
    
}
console.log(Narray);
//hecho en clase
